# Tauri bridge (archived)

The Tauri desktop shell and React frontend were removed from this repository. There is **no** Tauri IPC surface in-tree.

**Use instead:** MCP tools in `src/bin/the-desk-mcp.rs` and Rust modules under `src/`.
