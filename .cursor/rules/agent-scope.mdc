---
description: Default agent scope — backend and MCP first
alwaysApply: true
---

# Agent Scope

**Default focus:** Rust crate at repository root (`Cargo.toml`, `src/`), MCP server, agent definitions, research, SQLite. Output in Cursor/Claude Code/Codex.

This repository does not contain a desktop or web UI; do not assume React, Tauri, or browser code paths.
